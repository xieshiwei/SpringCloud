server:
  port: 8001

mybatis:
  check-config-location: classpath:mybatis/mybatis.cfg.xml # mybatis配置文件所在目录
  type-aliases-package: com.entity #所有entity的别名累所在包
  mapper-locations: classpath:mybatis/mapper/**/*.xml # mapper映射

  #数据源
  datasource:
    url: jdbc:mysql://47.100.240.175:3306/vehicleplatform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: now
    password: Heycars@2019
    druid:
      initial-size: 5 #连接池初始化大小
      min-idle: 10 #最小空闲连接数
      max-active: 20 #最大连接数
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
      stat-view-servlet: #访问监控网页的登录用户名和密码
        login-username: druid
        login-password: druid

  # mybatis
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.vehicle.platform.dao
    #在控制台打印SQL语句
    #configuration:
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

spring:
  application:
    name: microservicecloud-dept # 对外暴露的微服务的名字
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.alibaba.druid.proxy.DruidDriver #mysql驱动包
    url: jdbc:mysql://localhost:3306/cloudDB01 #数据库名字
    data-username: root
    data-password: 123456
    dbcp2:
      min-idle: 5 #数据库连接池的最小维持连接数
      initial-size: 5 #初始化连接数
      max-total: 5 # 最大连接数
      max-wait-millis: 200 #等待连续获取的最大超时时间
